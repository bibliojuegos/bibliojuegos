//recuerda no colocar 'Ñ' en los nombres de los atributos ni tampoco que empiecen con mayuscula
//Juegos
create table juegos(
idJuego numeric(3),
nombre varchar(30),
idPlataforma numeric(3),
idioma varchar(20),
ano numeric(4),
restriccion varchar(20),
idCompania numeric(3),
idGenero numeric(3),
stock numeric(4),
precio numeric(6),
primary key(idJuego)
);

create table cliente
  (
    idCliente numeric (3) not null ,
    nombre    varchar (20) ,
    apellido  varchar (20) ,
    edad      numeric (2) ,
    idVentas  numeric (10),
    telefono varchar (20)
  ) ;


create table compania
  (
    idCompania numeric (3) not null ,
    nombre     varchar (20) ,
    idPais     numeric (3)
  ) ;


create table empleados
  (
    idEmpleado numeric (3) not null ,
    rut        varchar (15) ,
    nombre     varchar (20) ,
    apellido   varchar (20) ,
    telefono   varchar (15) ,
    idSucursal numeric (3)
  ) ;


create table genero
  (
    idGenero numeric (3) not null ,
    nombre   varchar (20)
  ) ;


create table pais
  (
    idPais numeric (3) not null ,
    nombre varchar (20)
  ) ;


create table plataforma
  (
    idPlataforma numeric (3) not null ,
    nombre       varchar (20) ,
    lanzamiento  numeric (4)
  ) ;


create table ventas
  (
    idVentas    numeric (10) not null ,
    cliente     numeric (3) ,
    sucursal    numeric (3) ,
    juego       numeric (3) ,
    idSucursal  numeric (3) ,
    totalCompra numeric (6)
  ) ;


create table sucursal
  (
    idSucursal numeric (3) not null ,
    direccion  varchar (30) ,
    telefono   varchar (15) ,
    email      varchar (25)
  ) ;


alter table cliente add constarint cliente_pk primary key
(
  idCliente
)
;

alter table compania add constarint cliente_pk primary key
(
  idCompania
)
;

alter table genero add constarint cliente_pk primary key
(
  idGenero
)
;

alter table juegos add constarint cliente_pk primary key
(
  idJuego
)
;

alter table pais add constarint cliente_pk primary key
(
  idPais
)
;

alter table plataforma add constarint cliente_pk primary key
(
  idPlataforma
)
;

alter table ventas add constarint cliente_pk primary key
(
  idVentas
)
;

alter table sucursal add constarint cliente_pk primary key
(
  idSucursal
)
;

//Datos genero:
insert into genero(idGenero,nombre) values(1,'RPG');
insert into genero(idGenero,nombre) values(2,'Shooter');
insert into genero(idGenero,nombre) values(3,'Deportes');
insert into genero(idGenero,nombre) values(4,'Accion');
insert into genero(idGenero,nombre) values(5,'Aventura');
insert into genero(idGenero,nombre) values(6,'Otome');

//Datos pais:
insert into pais(idPais,nombre) values(1,'Japon');
insert into pais(idPais,nombre) values(2,'Estados unidos');
insert into pais(idPais,nombre) values(3,'Reino unido');

//Datos compania:
insert into compania(idCompania,nombre,idPais) values(1,'Konami',1);
insert into compania(idCompania,nombre,idPais) values(2,'Square Enix',1);
insert into compania(idCompania,nombre,idPais) values(3,'Capcom',1);
insert into compania(idCompania,nombre,idPais) values(4,'Atlus',1);
insert into compania(idCompania,nombre,idPais) values(5,'Blizzard',2);
insert into compania(idCompania,nombre,idPais) values(6,'Rockstar',3);
insert into compania(idCompania,nombre,idPais) values(7,'Activision',2);
insert into compania(idCompania,nombre,idPais) values(8,'Namco Bandai',1);

//Datos plataforma:
insert into plataforma(idPlataforma,nombre,lanzamiento) values(1,'PS3',2006);
insert into plataforma(idPlataforma,nombre,lanzamiento) values(2,'XBOX360',2005);
insert into plataforma(idPlataforma,nombre,lanzamiento) values(3,'PSVITA',2006);
insert into plataforma(idPlataforma,nombre,lanzamiento) values(4,'3DS',2006);
insert into plataforma(idPlataforma,nombre,lanzamiento) values(5,'PC',1982);
